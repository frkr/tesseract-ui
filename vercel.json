{
	"buildCommand": "pnpm build",
	"devCommand": "pnpm dev",
	"installCommand": "pnpm install --frozen-lockfile",
	"framework": "sveltekit",
	"outputDirectory": ".vercel/output",
	"ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"pre\" ]; then exit 1; else exit 0; fi"
}
