{
	"buildCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"pre\" ]]; then pnpm db:push; fi && pnpm build",
	"devCommand": "pnpm dev",
	"installCommand": "pnpm install",
	"framework": "sveltekit",
	"outputDirectory": ".vercel/output",
	"ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" = \"staging\"] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"dev\" ]]; then exit 0; else exit 1; fi"
}
