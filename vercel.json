{
	"buildCommand": "node -e \"process.env.VERCEL_ENV === 'preview' && require('child_process').execSync('echo 'y' | pnpm db:push')\" && pnpm build",
	"devCommand": "pnpm dev",
	"installCommand": "pnpm install",
	"framework": "sveltekit",
	"outputDirectory": ".vercel/output"
}
