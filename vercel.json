{
	"buildCommand": "if [ \"$VERCEL_ENV\" = \"preview\" ] || [ \"$VERCEL_ENV\" = \"production\" ]; then pnpm drizzle-kit push --force; fi && pnpm build",
	"devCommand": "pnpm dev",
	"installCommand": "pnpm install",
	"framework": "sveltekit",
	"outputDirectory": ".vercel/output"
}
